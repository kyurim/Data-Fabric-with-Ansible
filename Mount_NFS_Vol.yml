---
- hosts: 115.144.174.37
  tasks:
  - name: install nfs client
    yum: name=nfs-utils state=present

  - name: verify mount directory
    file:
     path: /mnt/test
     state: directory

  - name: mount nfs vol
    ansible.posix.mount:
      state: mounted
      path: /mnt/test
      src: 115.144.174.92:/ans_demo_vol1
      fstype: nfs
