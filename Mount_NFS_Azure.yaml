---
- hosts: 20.196.203.209
  tasks:
  - name: install nfs client
    yum: name=nfs-utils state=present

  - name: verify mount directory
    file:
     path: /mnt/test
     state: directory

  - name: mount nfs vol
    mount:
      state: mounted
      path: /mnt/test
      src: 20.214.75.15:/target_vol_clone
      fstype: nfs

